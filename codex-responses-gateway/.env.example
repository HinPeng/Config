# Local gateway key; use the same value in the client's Gateway provider.
CODEX_GATEWAY_API_KEY=replace-with-a-long-random-local-key

# Upstream URLs and keys are read from CC Switch at startup.
# Requires Python 3.11+; optionally specify PYTHON_BIN=/path/to/python3
